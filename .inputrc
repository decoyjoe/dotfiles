# Use Ctrl+X, Ctrl+R to re-read ~/.inputrc

# Load distro's default inputrc
$include /etc/inputrc

# Search history for items that start with the current input when using up/down arrows
"\e[A": history-search-backward
"\e[B": history-search-forward

